const pg = require('pg');

pg.pool();
// get querying for and aggregating product reviews

// insert query a review for a product
// run function or file that update the meta data
// includes ratings counts, recommended/ not recommended counts, total number of reviews
// and total score for characteristic reviews
